/** Shared scene constants. */
export const SUN_DIRECTION: [number, number, number] = [
  -0.42, 0.78, 0.46,
]

export const FOG_COLOR = '#cfe4f8'

/** Launch pad geometry, in meters. */
export const PAD = {
  apronRadius: 125,
  poolRadius: 41,
  padRadius: 30,
  padHeight: 2.2,
  mountHeight: 7,
}
